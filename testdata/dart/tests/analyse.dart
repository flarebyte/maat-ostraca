void main() {
  group('auth', () {
    test("logs in", () {});
    testWidgets('shows home', (tester) async {});
  });

  test(title, () {});
  group("hello $name", () {});
  test('x' + suffix, () {});
}
