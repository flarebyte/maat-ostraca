void main() {
  if (a) {}
  else if (b) {}

  switch (kind) {
    case 1:
      break;
    case 2:
      break;
  }

  final first = ok ? 1 : 2;
  final second = ready ? one : two;
  print(first + second);
}
